'use server';import { revalidatePath } from 'next/cache';import { redirect } from 'next/navigation';import { getBusiness, nextOrderNumber } from '@/lib/supabase/data';
const val=(f:FormData,k:string)=>String(f.get(k)||'');
export async function saveCustomer(form:FormData){const {supabase,business}=await getBusiness();const id=val(form,'id');const row={business_id:business.id,name:val(form,'name'),phone:val(form,'phone'),whatsapp_number:val(form,'whatsapp_number'),email:val(form,'email'),address:val(form,'address'),city:val(form,'city'),state:val(form,'state'),notes:val(form,'notes')};if(id)await supabase.from('customers').update(row).eq('id',id).eq('business_id',business.id);else await supabase.from('customers').insert(row);revalidatePath('/app/customers');redirect('/app/customers')}
export async function deleteCustomer(form:FormData){const {supabase,business}=await getBusiness();await supabase.from('customers').delete().eq('id',val(form,'id')).eq('business_id',business.id);redirect('/app/customers')}
export async function saveProduct(form:FormData){const {supabase,business}=await getBusiness();const id=val(form,'id');const row={business_id:business.id,name:val(form,'name'),description:val(form,'description'),sku:val(form,'sku'),price:Number(val(form,'price')),image_url:val(form,'image_url'),active:form.get('active')==='on',stock_quantity:Number(val(form,'stock_quantity')||0)};if(id)await supabase.from('products').update(row).eq('id',id).eq('business_id',business.id);else await supabase.from('products').insert(row);redirect('/app/products')}
export async function createOrder(form:FormData){const {supabase,business}=await getBusiness();let customerId=val(form,'customer_id');if(!customerId){const {data:c}=await supabase.from('customers').insert({business_id:business.id,name:val(form,'customer_name'),phone:val(form,'customer_phone'),whatsapp_number:val(form,'customer_phone')}).select().single();customerId=c.id}const productIds=form.getAll('product_id').map(String),qtys=form.getAll('quantity').map(Number);let subtotal=0;const items=[];for(let i=0;i<productIds.length;i++){const {data:p}=await supabase.from('products').select('*').eq('id',productIds[i]).eq('business_id',business.id).single();const quantity=qtys[i]||1,total=Number(p.price)*quantity;subtotal+=total;items.push({product_id:p.id,product_name:p.name,quantity,unit_price:p.price,total_price:total})}const delivery_fee=Number(val(form,'delivery_fee')||0),discount=Number(val(form,'discount')||0),total=subtotal+delivery_fee-discount;const {data:o}=await supabase.from('orders').insert({business_id:business.id,customer_id:customerId,order_number:await nextOrderNumber(supabase,business.id),status:val(form,'status')||'New',subtotal,delivery_fee,discount,total,payment_status:val(form,'payment_status')||'Unpaid',payment_method:val(form,'payment_method')||'Bank Transfer',notes:val(form,'notes')}).select().single();await supabase.from('order_items').insert(items.map(x=>({...x,order_id:o.id})));redirect(`/app/orders/${o.id}`)}
export async function updateOrder(form:FormData){const {supabase,business}=await getBusiness();await supabase.from('orders').update({status:val(form,'status'),payment_status:val(form,'payment_status'),payment_method:val(form,'payment_method'),notes:val(form,'notes')}).eq('id',val(form,'id')).eq('business_id',business.id);revalidatePath(`/app/orders/${val(form,'id')}`)}
export async function saveSettings(form:FormData){const {supabase,business,user}=await getBusiness();await supabase.from('businesses').update({name:val(form,'name'),category:val(form,'category'),phone:val(form,'phone'),whatsapp_number:val(form,'whatsapp_number'),email:val(form,'email'),address:val(form,'address'),state:val(form,'state'),city:val(form,'city'),logo_url:val(form,'logo_url'),currency:'NGN'}).eq('id',business.id);await supabase.from('profiles').upsert({user_id:user.id,full_name:val(form,'full_name')});revalidatePath('/app/settings')}
