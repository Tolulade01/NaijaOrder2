import { saveCustomer, saveProduct } from '@/app/app/actions';
export function CustomerForm({c={}}:{c?:any}){return <form action={saveCustomer} className="card space-y-3 p-4"><input type="hidden" name="id" defaultValue={c.id}/>{['name','phone','whatsapp_number','email','address','city','state','notes'].map(k=><label className="label capitalize" key={k}>{k.replace('_',' ')}<input className="input" name={k} required={k==='name'} defaultValue={c[k]}/></label>)}<button className="btn btn-primary">Save Customer</button></form>}
export function ProductForm({p={active:true}}:{p?:any}){return <form action={saveProduct} className="card space-y-3 p-4"><input type="hidden" name="id" defaultValue={p.id}/>{['name','description','sku','price','image_url','stock_quantity'].map(k=><label className="label capitalize" key={k}>{k.replace('_',' ')}<input className="input" name={k} type={k==='price'||k==='stock_quantity'?'number':'text'} step="0.01" required={k==='name'||k==='price'} defaultValue={p[k]}/></label>)}<label className="flex gap-2"><input name="active" type="checkbox" defaultChecked={p.active}/> Active</label><button className="btn btn-primary">Save Product</button></form>}
